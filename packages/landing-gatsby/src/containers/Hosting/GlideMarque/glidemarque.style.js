import styled from 'styled-components';

const GlideMarqueWrapper = styled.section`
  marquee ul {
    display: flex;
    li {
      img {
        max-width: 100px;
      }
    }
  }
`;

export default GlideMarqueWrapper;
